module shifter (
    input a[16],
    input b[4],
    input alufn_signal[5],
    output out[16]
  ) {

  always {
  case (alufn_signal){
    default: out = a;
    b01100: out = a << b;
    b01101: out = a >> b;
    b01110: out = $signed(a) >>> b;
    }
  }
}