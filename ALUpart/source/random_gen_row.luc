module random_gen_row (
    input clk,  // clock
    input rst,  // reset
    input trigger,
    output out[16]
  ) {
  pn_gen pn_gen1(.clk(clk),.rst(rst),.seed(32h100)); 
  
  always {
 
    pn_gen1.next = trigger;
    out = pn_gen1.num[3:0];
 
    }
}